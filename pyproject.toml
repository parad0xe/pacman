[project]
name = "pacman"
version = "0.0.1"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.13.2",
    "raylib>=5.5.0.4",
]

[dependency-groups]
dev = [
    "flake8>=7.3.0",
    "mypy>=1.19.1",
]
